fastapi==0.115.6
uvicorn[standard]==0.34.0
anthropic==0.52.0
python-multipart==0.0.20
pypdf==5.4.0
pypdfium2==5.7.1
Pillow==12.1.1
python-docx==1.1.2
python-pptx==1.0.2
# 산출내역서 .xlsx 다운로드 (B2G 입찰 양식 정합)
openpyxl==3.1.5
psycopg[binary]==3.2.10
# Cloudflare R2 (S3 호환) — 마스터 PPTX 클라우드 스토리지
boto3==1.35.92
# 베타 인증 — 묶음 N
bcrypt==4.1.2
PyJWT==2.8.0
# Spec D-Fix-8 (5/18) — 이메일 인증 (Resend SDK, noreply@nightoff.ai)
resend==2.0.0
# Phase 4 — 매일 00:00 KST quota 자동 리셋 (BackgroundScheduler 데몬 스레드).
# 미설치 시 graceful skip → 어드민 수동 트리거(설정 탭 버튼)로 대체 가능.
apscheduler==3.10.4
# RAG (옵셔널) — 두 패키지 설치 실패해도 NightOff 자체는 정상 동작 (rag_retriever 가 graceful skip).
# Railway nixpacks 의 phases.install 에서 best-effort 로 설치하도록 분리.
# 로컬 개발 시: pip install -r requirements-rag.txt
# Spec D-Build-HTMLOutput — HTML → PPTX 변환 (Playwright 헤드리스 chromium 렌더)
# admin 전용 토글 (policy_settings.output_mode_enabled='Y') 활성 시에만 사용. OFF 시 미사용.
# 설치 후: playwright install chromium
playwright==1.60.0
