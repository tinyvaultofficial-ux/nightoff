"use client"

import { cn } from "@/lib/utils"
import {
  Users,
  FileText,
  MessageSquare,
  Calendar,
  Settings,
  BarChart3,
  FolderOpen,
  Search,
  Plus,
  ChevronDown,
} from "lucide-react"
import { useState } from "react"

const navigation = [
  { name: "대시보드", href: "#", icon: BarChart3, current: false },
  { name: "클라이언트", href: "#", icon: Users, current: true },
  { name: "제안서", href: "#", icon: FileText, current: false },
  { name: "대화기록", href: "#", icon: MessageSquare, current: false },
  { name: "일정관리", href: "#", icon: Calendar, current: false },
  { name: "자료실", href: "#", icon: FolderOpen, current: false },
]

const recentClients = [
  { name: "삼성전자", color: "bg-blue-500" },
  { name: "현대자동차", color: "bg-emerald-500" },
  { name: "LG에너지솔루션", color: "bg-orange-500" },
]

export function Sidebar() {
  const [isWorkspaceOpen, setIsWorkspaceOpen] = useState(true)

  return (
    <aside className="fixed left-0 top-0 flex h-screen w-64 flex-col border-r border-sidebar-border bg-sidebar">
      {/* Logo */}
      <div className="flex h-16 items-center gap-2.5 border-b border-sidebar-border px-5">
        <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-primary">
          <FileText className="h-4.5 w-4.5 text-primary-foreground" />
        </div>
        <span className="text-lg font-semibold tracking-tight text-sidebar-foreground">
          ProposalHub
        </span>
      </div>

      {/* Search */}
      <div className="p-4">
        <div className="flex items-center gap-2 rounded-lg bg-muted/60 px-3 py-2.5 transition-colors hover:bg-muted">
          <Search className="h-4 w-4 text-muted-foreground" />
          <span className="text-sm text-muted-foreground">검색...</span>
          <kbd className="ml-auto rounded bg-background px-1.5 py-0.5 text-[10px] font-medium text-muted-foreground shadow-sm">
            ⌘K
          </kbd>
        </div>
      </div>

      {/* Navigation */}
      <nav className="flex-1 overflow-y-auto px-3">
        <ul className="space-y-1">
          {navigation.map((item) => (
            <li key={item.name}>
              <a
                href={item.href}
                className={cn(
                  "group flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium transition-all",
                  item.current
                    ? "bg-sidebar-accent text-sidebar-primary"
                    : "text-sidebar-foreground/70 hover:bg-sidebar-accent/50 hover:text-sidebar-foreground"
                )}
              >
                <item.icon
                  className={cn(
                    "h-[18px] w-[18px] transition-colors",
                    item.current
                      ? "text-sidebar-primary"
                      : "text-muted-foreground group-hover:text-sidebar-foreground"
                  )}
                />
                {item.name}
              </a>
            </li>
          ))}
        </ul>

        {/* Recent Clients Section */}
        <div className="mt-8">
          <button
            onClick={() => setIsWorkspaceOpen(!isWorkspaceOpen)}
            className="flex w-full items-center justify-between px-3 py-2 text-xs font-semibold uppercase tracking-wider text-muted-foreground hover:text-sidebar-foreground"
          >
            최근 클라이언트
            <ChevronDown
              className={cn(
                "h-3.5 w-3.5 transition-transform",
                isWorkspaceOpen && "rotate-180"
              )}
            />
          </button>
          {isWorkspaceOpen && (
            <ul className="mt-1 space-y-1">
              {recentClients.map((client) => (
                <li key={client.name}>
                  <a
                    href="#"
                    className="flex items-center gap-3 rounded-lg px-3 py-2 text-sm text-sidebar-foreground/70 transition-colors hover:bg-sidebar-accent/50 hover:text-sidebar-foreground"
                  >
                    <span
                      className={cn(
                        "h-2 w-2 rounded-full",
                        client.color
                      )}
                    />
                    {client.name}
                  </a>
                </li>
              ))}
            </ul>
          )}
        </div>
      </nav>

      {/* New Client Button */}
      <div className="border-t border-sidebar-border p-4">
        <button className="flex w-full items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground shadow-sm transition-all hover:bg-primary/90 hover:shadow">
          <Plus className="h-4 w-4" />
          새 클라이언트
        </button>
      </div>

      {/* User */}
      <div className="border-t border-sidebar-border p-4">
        <div className="flex items-center gap-3">
          <div className="flex h-9 w-9 items-center justify-center rounded-full bg-gradient-to-br from-primary/80 to-primary text-sm font-semibold text-primary-foreground">
            김
          </div>
          <div className="flex-1 min-w-0">
            <p className="truncate text-sm font-medium text-sidebar-foreground">
              김제안
            </p>
            <p className="truncate text-xs text-muted-foreground">
              제안전문가
            </p>
          </div>
          <button className="rounded-lg p-1.5 text-muted-foreground transition-colors hover:bg-sidebar-accent hover:text-sidebar-foreground">
            <Settings className="h-4 w-4" />
          </button>
        </div>
      </div>
    </aside>
  )
}
